// ltl-log-lib/main.ltl
//
// Structured logger (key=value + JSON) with log levels.
//
// This is an early skeleton — contribution is welcome! See the
// README for build instructions.

import std::io
import std::fmt

pub fn main(args: [str]) -> Int {
    io::println("ltl-log-lib v0.1.0")
    io::println("Structured logger (key=value + JSON) with log levels.")
    0
}
